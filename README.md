# MeTenTen (Marriage Encounter 10&10) 웹앱

Marriage Encounter 10&10 프로그램을 위한 웹 기반 애플리케이션입니다. 부부가 매일 10분간 편지를 쓰고 10분간 대화를 나누며 서로의 마음을 깊이 이해할 수 있도록 돕습니다.

## 📋 주요 기능

### 🔐 보안 및 인증
- **이메일/비밀번호 기반 인증** (Firebase Authentication)
- **엔드투엔드 암호화** (E2EE)
  - PBKDF2 (100,000 iterations, SHA-256)
  - AES-256-CBC 암호화
  - 사용자 비밀번호로 암호화된 DEK (Data Encryption Key)
- **개인/공유 암호화 모드**
  - 개인: 본인만 읽을 수 있는 10&10
  - 공유: 배우자와 공유하는 10&10

### 💑 10&10 시스템
- **주제(Topic) 관리**
  - 자유로운 주제 생성 및 관리
  - 월별 필터링
  - 완료/미완료 상태 추적
- **10&10 작성**
  - 10분 타이머 기능
  - 최대 5000자 입력
  - 개인/공유 암호화 타입 선택
  - 작성 후 수정 불가 (무결성 보장)
- **배우자 10&10 조회**
  - 공유 타입 10&10만 조회 가능
  - 실시간 동기화

### 👥 배우자 연결
- **이메일로 배우자 초대**
- **공유 DEK 자동 생성 및 교환**
- **배우자 연결 해제 기능**
- **Pending Shared DEK 자동 처리**

### 💭 보조 기능
- **느낌 표현 (65개)**
  - 4가지 카테고리: 기쁨, 두려움, 분노, 슬픔
  - 사용자 정의 예시 추가/삭제
  - 기본 예시는 삭제 불가
- **기도문 (6개)**
  - 3가지 카테고리: 부부, 가정, 교회
  - 클립보드 복사 기능
  - 즐겨찾기 및 조회수 추적

### 🎨 UI/UX
- **반응형 디자인** (모바일, 태블릿, 데스크톱)
- **다크 모드 미지원** (현재 버전)
- **접근성** (ARIA 레이블, 키보드 네비게이션)
- **한국어 지원**

## 🛠 기술 스택

### Frontend
- **React 18** + **TypeScript**
- **Vite** (빌드 도구)
- **React Router v6** (라우팅)
- **Zustand** (상태 관리)
- **Ant Design** (UI 라이브러리)
- **date-fns** (날짜 처리)

### Backend & Database
- **Firebase Authentication** (사용자 인증)
- **Firebase Realtime Database** (데이터 저장)
- **Web Crypto API** (암호화)

### Development Tools
- **ESLint** + **Prettier** (코드 품질)
- **TypeScript** (타입 안정성)

## 📦 설치 및 실행

### 사전 요구사항
- Node.js 18.x 이상
- npm 또는 yarn
- Firebase 프로젝트 (Authentication, Realtime Database 활성화)

### 1. 저장소 클론
```bash
git clone https://github.com/your-username/MeTenTen.git
cd MeTenTen/webapp
```

### 2. 의존성 설치
```bash
npm install
```

### 3. 환경 변수 설정
`.env.example` 파일을 복사하여 `.env` 파일을 생성하고, Firebase 설정 값을 입력합니다.

```bash
cp .env.example .env
```

`.env` 파일 편집:
```env
VITE_FIREBASE_API_KEY=your_actual_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_DATABASE_URL=https://your_project_id-default-rtdb.firebaseio.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
VITE_FIREBASE_APP_ID=your_app_id
```

### 4. Firebase Security Rules 설정
`FIREBASE_SECURITY_RULES.md` 파일을 참고하여 Firebase Console에서 Security Rules를 설정합니다.

### 5. 개발 서버 실행
```bash
npm run dev
```

브라우저에서 `http://localhost:5173` 열기

### 6. 프로덕션 빌드
```bash
npm run build
npm run preview  # 빌드 결과 미리보기
```

## 🔒 보안 아키텍처

### 암호화 흐름

#### 회원가입
1. 사용자가 이메일/비밀번호 입력
2. Random DEK (256-bit) 생성
3. 비밀번호로 DEK 암호화 (PBKDF2 + AES-256-CBC)
4. 암호화된 DEK를 Firebase에 저장
5. Firebase Authentication에 사용자 등록

#### 로그인
1. 사용자가 이메일/비밀번호 입력
2. Firebase Authentication 인증
3. 암호화된 DEK 조회
4. 비밀번호로 DEK 복호화
5. DEK를 메모리에만 보관 (로컬 스토리지/세션 스토리지 사용 안 함)

#### 10&10 작성 (개인)
1. 사용자가 내용 작성
2. 메모리의 DEK로 내용 암호화
3. 암호화된 데이터를 Firebase에 저장

#### 10&10 작성 (공유)
1. 사용자가 내용 작성
2. 메모리의 공유 DEK로 내용 암호화
3. 암호화된 데이터를 Firebase에 저장
4. 배우자가 공유 DEK로 복호화하여 읽기 가능

#### 배우자 초대
1. 초대자가 배우자 이메일 입력
2. Random 공유 DEK 생성
3. 초대자 DEK로 공유 DEK 암호화 → 초대자 파트너 정보에 저장
4. 피초대자 DEK로 공유 DEK 암호화 → pending_shared_deks에 저장
5. 피초대자 로그인 시 자동으로 pending_shared_deks 처리

### 보안 원칙
- **Zero Knowledge**: 서버는 평문 데이터를 절대 볼 수 없음
- **End-to-End Encryption**: 클라이언트에서만 암호화/복호화
- **Password-Based Encryption**: DEK는 사용자 비밀번호로만 복호화 가능
- **No Key Escrow**: 비밀번호를 잊으면 데이터 복구 불가능

## 📁 프로젝트 구조

```
webapp/
├── public/              # 정적 파일
├── src/
│   ├── components/      # 재사용 가능한 컴포넌트
│   │   ├── Layout.tsx
│   │   ├── ErrorBoundary.tsx
│   │   ├── Loading.tsx
│   │   ├── TenTenModal.tsx
│   │   └── ...
│   ├── pages/           # 페이지 컴포넌트
│   │   ├── Login.tsx
│   │   ├── Home.tsx
│   │   ├── MyTenTens.tsx
│   │   └── ...
│   ├── services/        # 비즈니스 로직
│   │   ├── AuthService.ts
│   │   ├── EncryptionService.ts
│   │   ├── FirebaseDataService.ts
│   │   ├── TopicService.ts
│   │   ├── TenTenService.ts
│   │   └── ...
│   ├── stores/          # Zustand 상태 관리
│   │   └── authStore.ts
│   ├── types/           # TypeScript 타입 정의
│   ├── utils/           # 유틸리티 함수
│   ├── config/          # 설정 파일
│   ├── styles/          # 전역 스타일
│   ├── App.tsx          # 루트 컴포넌트
│   └── main.tsx         # 엔트리 포인트
├── .env.example         # 환경 변수 템플릿
├── package.json
├── tsconfig.json
├── vite.config.ts
└── README.md
```

## 🧪 테스트

### 수동 테스트 체크리스트

#### 인증
- [ ] 회원가입
- [ ] 로그인
- [ ] 로그아웃
- [ ] 비밀번호 변경

#### 10&10 시스템
- [ ] 주제 생성/수정/삭제
- [ ] 10&10 작성 (개인 타입)
- [ ] 10&10 작성 (공유 타입)
- [ ] 10&10 조회
- [ ] 월별 필터링

#### 배우자 연결
- [ ] 배우자 초대
- [ ] 배우자 10&10 조회
- [ ] 배우자 연결 해제

#### 느낌 표현 & 기도문
- [ ] 느낌 표현 조회 및 추가
- [ ] 기도문 조회 및 복사

## 🚀 배포

### Firebase Hosting 배포

1. Firebase CLI 설치
```bash
npm install -g firebase-tools
```

2. Firebase 로그인
```bash
firebase login
```

3. Firebase 프로젝트 초기화
```bash
firebase init hosting
```

4. 빌드 및 배포
```bash
npm run build
firebase deploy
```

## 🤝 기여

이 프로젝트는 Marriage Encounter 10&10 프로그램을 위한 개인 프로젝트입니다.

## 📄 라이선스

이 프로젝트는 개인 사용 목적으로 개발되었습니다.

## 📧 문의

프로젝트 관련 문의사항이 있으시면 Issues를 통해 연락 주세요.

## 🎯 로드맵

### 완료된 기능
- ✅ 사용자 인증 시스템
- ✅ E2E 암호화
- ✅ 10&10 시스템
- ✅ 배우자 연결
- ✅ 느낌 표현
- ✅ 기도문
- ✅ 반응형 디자인
- ✅ 접근성 개선

### 향후 계획
- [ ] PWA 지원 (오프라인 모드)
- [ ] 푸시 알림 (10&10 리마인더)
- [ ] 데이터 백업/복원
- [ ] 통계 및 인사이트
- [ ] 다국어 지원

## ⚠️ 주의사항

1. **비밀번호 보안**: 비밀번호를 잊으면 모든 암호화된 데이터를 복구할 수 없습니다.
2. **백업**: 정기적으로 중요한 10&10을 별도로 백업하는 것을 권장합니다.
3. **브라우저 호환성**: Chrome, Firefox, Safari, Edge 최신 버전 권장
4. **HTTPS 필수**: 프로덕션 환경에서는 반드시 HTTPS 사용

## 🙏 감사의 말

Marriage Encounter 운동과 10&10 프로그램을 만들어주신 모든 분들께 감사드립니다.





